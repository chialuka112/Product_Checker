PRDCheck – Product Checker

PRDCheck is a simple web app that lets businesses register products and generate unique tracking codes. Users can enter the code to verify if a product is real.

🔍 Features

Register products

Auto-generate unique codes

Verify product authenticity

React frontend + Node/Express backend

Supports PostgreSQL

🚀 Getting Started
Clone
git clone https://github.com/chialuka112/prdcheck.git

Frontend
cd frontend
npm install
npm run dev

Backend
cd backend
npm install
npm run dev

🔑 Environment

Create .env in backend:

DATABASE_URL=your_database_url
PORT=5000
